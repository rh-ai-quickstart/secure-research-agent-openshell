# OpenShell sandbox egress policy for the AIQ research agent.
#
# All NVIDIA traffic (LLM chat and embeddings) is routed through
# inference.local via the standalone inference routes file baked into the
# image. The NVIDIA API key is injected by the supervisor and never
# enters the sandbox process's environment.
#
# Tavily (web search) is allowed as a direct endpoint.
# To add additional services, append entries under network_policies.

version: 1

filesystem_policy:
  include_workdir: true
  read_only: [/usr, /lib, /proc, /dev/urandom, /app, /etc, /var/log]
  read_write: [/sandbox, /tmp, /dev/null, /etc/hosts]

landlock:
  compatibility: best_effort

process:
  run_as_user: sandbox
  run_as_group: sandbox

network_policies:
  aiq_tavily:
    name: aiq_tavily
    endpoints:
      - { host: api.tavily.com, port: 443 }
    binaries:
      - { path: /app/.venv/bin/python }
      - { path: /app/.venv/bin/python3.12 }
      - { path: /usr/bin/python3.12 }
      - { path: /app/.venv/bin/dask-worker }
      - { path: /app/.venv/bin/dask-scheduler }
      - { path: /usr/bin/curl }
